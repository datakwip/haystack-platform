# Local Docker Development Environment Variables

# Database Configuration (connects to docker-compose timescaledb service)
DATABASE_URL=postgresql://datakwip_user:datakwip_password@localhost:5432/datakwip

# Service Configuration
HEALTH_PORT=8080
LOG_LEVEL=INFO

# Note: When running the simulator OUTSIDE docker but connecting to dockerized DB,
# use localhost. When running INSIDE docker, use service name (timescaledb).
