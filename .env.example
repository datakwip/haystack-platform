# Environment Variables for Haystack Data Simulator Service
#
# Copy this file to .env and update with your actual values

# ===== Database Configuration =====
# Option 1: Use full DATABASE_URL (Railway format)
DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Use individual database settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=datakwip
DB_USER=demo
DB_PASSWORD=demo123

# ===== Service Configuration =====
# Data generation interval (minutes)
SERVICE_INTERVAL_MINUTES=15

# Health check server port (for Railway)
HEALTH_CHECK_PORT=8080

# ===== Configuration File Paths =====
# (Optional - defaults shown)
DB_CONFIG_PATH=config/database_config.yaml
BUILDING_CONFIG_PATH=config/building_config.yaml

# ===== Logging Configuration =====
LOG_LEVEL=INFO

# ===== Railway-Specific Variables =====
# These are typically provided by Railway automatically
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=your-project-id
# RAILWAY_SERVICE_ID=your-service-id
